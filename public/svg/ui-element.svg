<svg version="1.1" xmlns="http://www.w3.org/2000/svg" height="30">

    <style>
        .color
        {
            fill: white;
        }
    </style>

    <filter id="filter">
        <feGaussianBlur stdDeviation="2 1" width="100%" in="flood" edgeMode="wrap" result="blur"/>
    </filter>

    <rect class="color" width="100%" height="30" filter="url(#filter)" />

</svg>
